🎯 مقدمه
این پروژه یک رابط کاربری گرافیکی (GUI) با استفاده از PySide6 فراهم می‌کند که به کاربر اجازه می‌دهد:

تصویر دلخواه خود را بارگذاری کند.

چهره یا چشم‌ها را با الگوریتم Haar Cascade تشخیص دهد.

یک فیلتر کانولوشن سفارشی (با کرنل 3×3) روی تصویر اعمال کند.

📂 ساختار پروژه
text
project/
├─ faceFilter.py   # ماژول اعمال فیلتر کانولوشن
├─ faceDetect.py   # ماژول تشخیص چهره/چشم
├─ gui.py          # رابط کاربری PySide6
├─ form.ui         # فایل طراحی رابط کاربری (Qt Designer)
└─ README.m
⚙️ نصب و اجرا
پیش‌نیازها
Python 3.9+

کتابخانه‌ها:
pip install PySide6 opencv-python numpy
اجرای برنامه:
python gui.py
🧩 ماژول‌ها
1. FaceFilter
وظیفه: اعمال یک فیلتر کانولوشن 3×3 روی تصویر ورودی.

ورودی‌ها: مسیر تصویر + ۹ عدد برای کرنل.

خروجی: تصویر جدید با نام output_filter.jpg.

مثال استفاده:
from faceFilter import FaceFilter
f = FaceFilter("image.jpg",1,0,-1,0,1,0,-1,0,1)
f.start()
2. FaceDetect
وظیفه: تشخیص چهره یا چشم‌ها با استفاده از Haar Cascade.

ورودی‌ها: مسیر تصویر + انتخاب ("Face" یا "Eyes").

خروجی: تصویر جدید با نام output.jpg که نواحی تشخیص داده شده با مستطیل قرمز مشخص شده‌اند.

مثال استفاده:
from faceDetect import FaceDetect
d = FaceDetect("image.jpg","Face")
d.start()
3. GUI
وظیفه: رابط کاربری برای بارگذاری تصویر، اجرای تشخیص چهره/چشم و اعمال فیلتر.

ویژگی‌ها:

دکمه انتخاب تصویر (pb1)

دکمه تشخیص چهره (pb3)

دکمه تشخیص چشم (pb4)

دکمه اعمال فیلتر (pb5)

خروجی: نمایش تصویر اصلی یا پردازش‌شده در QLabel
📌 نکات مهم
فایل‌های Haar Cascade (haarcascade_frontalface_alt.xml و haarcascade_eye.xml) باید در مسیر پروژه قرار داشته باشند.

نتایج پردازش در فایل‌های output.jpg و output_filter.jpg ذخیره می‌شوند.

برای طراحی رابط کاربری از Qt Designer استفاده شده و فایل form.ui باید در کنار کد باشد.
📜 لایسنس
این پروژه آزاد است و می‌توانید آن را تغییر دهید و استفاده کنید.
